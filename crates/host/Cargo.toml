[package]
name = "semi-os-host"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
semi-os-protocol = { path = "../protocol" }
semi-os-storage = { path = "../storage" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
