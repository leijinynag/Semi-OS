{
  "name": "@semi-os/agent-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@semi-os/protocol": "workspace:*",
    "@semi-os/shared": "workspace:*",
    "@semi-os/tool-sdk": "workspace:*",
    "typebox": "1.3.7"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/**/*.test.ts",
    "lint": "node --check src/index.ts",
    "format": "node --check src/index.ts",
    "typecheck": "tsc -p tsconfig.json"
  }
}
